apiVersion: v1
kind: Service
metadata:
  name: rest-svc
  labels:
    app: rest
spec:
  type: ClusterIP
  selector:
    app: rest
  ports:
  - name: rest-svc-port
    port: 8080
    protocol: TCP
    targetPort: 8080
