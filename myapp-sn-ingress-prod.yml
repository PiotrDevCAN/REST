apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    nginx.ingress.kubernetes.io/proxy-connect-timeout: "3600"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "3600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "3600"
  name: rest-ingress
  namespace: ns-rest-gd-kps-prd-wus3
spec:
  rules:
  - host: rest.zkpw3uspriv.app.kyndryl.net
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: rest-svc
            port:
              number: 8080
